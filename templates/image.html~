{% load static %}<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Image-List</title>
    <link rel="stylesheet" href="{% static 'css/bootstrap.min.css' %}">
  </head>
  <body>
    <div class="container">
      <ol class="breadcrumb my-4">
        <li class="breadcrumb-item"><a href="{% url 'home' %}">Plants</a></li>
        <li class="breadcrumb-item active">{{sc}}</li>        
      </ol>
    </div>
   <table class="table">
        {% for n in image %}
         <tr>
            <td align="center">{{n.image_name}}</th>
         </tr>
        {% endfor %}
             
   </table>
            
</body>
</html>
