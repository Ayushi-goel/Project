
{% load static %}<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{ spec}}</title>
    <link rel="stylesheet" href="{% static 'css/bootstrap.min.css' %}">
  </head>
  <body>
    <div class="container">
      <ol class="breadcrumb my-4">
        <li class="breadcrumb-item"><a href="{% url 'home' %}">Plants</a></li>
        <li class="breadcrumb-item active">{{pk}}</li>        
      </ol>
    </div>
          <table class="table">
        
         <tr>
            <th>Family</th>
            <td>{{family}}</td>
          </tr>
          <tr>
            <th>Scientific_Name</th>
            <td>{{Scien}}</td>
          </tr>
          <tr>
            <th>Synonym</th>
            <td>{{Syno}}</td>
          </tr>
          <tr>
            <th>species_id</th>
            <td>{{spec}}</td>
          </tr>     

        <tbody>
      </table>
     <li align=center><a href="{% url 'plant_image' spec %}">IMAGE-LIST</a></li>  

  </body>
</html>
